machine:
  xcode:
    version: 8.2
test:
    override:
        - uname -a
        - pwd
        - ls -a
        - echo "#### /usr/local"
        - find /usr/local
        - echo "#### opt"
        - find /opt
